# ================================================
# Employee Service - Docker Profile Configuration
# ================================================
# 이 파일은 Docker 환경에서 사용됩니다.
# 활성화: SPRING_PROFILES_ACTIVE=docker
# ================================================

# MySQL Configuration (Docker Network)
spring.datasource.url=jdbc:mysql://mysql:3306/erp_employee?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
